let () = 
  ()
(* if Array.length Sys.argv > 1 then
  match Sys.argv.(1) with
  | "-v" -> Matcher.Globals.verbose := true
  | "" -> ()
  |  _ -> failwith "Cli arg not recognised"

  Test.run_suite (); *)